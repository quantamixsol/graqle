# Reel 3: "One Vector Call"

**Duration:** 35 seconds
**Series:** 3 of 7
**Hashtags:** #Embeddings #AI #SemanticSearch #DevTools #GraQle
**Background visual:** `node-square.png` (middle purple ring focus)

---

## Storyboard

| Time | You (Camera) | Background | On-Screen Text |
|------|-------------|------------|----------------|
| 0-3s | Direct look. Confident. | Dark with purple accent | **"Layer 2: Embeddings"** (purple) |
| 3-9s | "Memory told us who's connected. Now — who's actually relevant?" | Purple ring from node-anatomy | (none) |
| 9-15s | "Your query gets embedded. One call. One vector." | Vector array floats: `[0.82, -0.14, 0.67...]` | **1 embedding call** |
| 15-21s | "That single vector gets compared against every chunk in the graph — already cached in a dot-NPZ file from the scan." | .npz file icon appears | **Pre-computed. Cached.** |
| 21-27s | "Auth: point-nine-four. Routes: point-eight-seven. Config: point-three-one — gone." | Similarity scores appear, config fades out | **Cosine similarity scoring** |
| 27-31s | "Eleven connected nodes. Eight survive. Cost: one hundred-thousandth of a cent." | 11 → 8 transition | **11 → 8 nodes · ~$0.00001** |
| 31-35s | "Two filters done. Still no LLM." | Hold. Pause. | **Still no LLM.** |

## Script (Dialogue)

> Memory told us who's connected. Now — who's actually relevant?
>
> Your query gets embedded. One call. One vector.
>
> That single vector gets compared against every chunk in the graph — already cached from the scan.
>
> Auth: point-nine-four. Routes: point-eight-seven. Config: point-three-one — gone.
>
> Eleven connected nodes. Eight survive. Cost: one hundred-thousandth of a cent.
>
> Two filters done. Still no LLM.

## Teleprompter

```
Memory told us who's connected.
Now — who's actually relevant?

Your query gets embedded.
One call. One vector.

That single vector gets compared
against every chunk in the graph —
already cached from the scan.

Auth: point nine four.
Routes: point eight seven.
Config: point three one — gone.

Eleven connected nodes.
Eight survive.
Cost: one hundred-thousandth of a cent.

Two filters done. Still no LLM.
```

## Edit Notes

- **9-15s:** The vector array `[0.82, -0.14, 0.67...]` should float in like code typing. Purple text on dark.
- **21-27s:** Show similarity scores next to node names. Config's score should visually fade/dim as you say "gone."
- **31-35s:** Hard pause. Let "Still no LLM" sit for 2 full seconds. This is the punchline of the episode.
- **Transition to next:** This drives curiosity — when DOES the LLM come in?
